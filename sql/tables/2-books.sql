CREATE TABLE books (
    isbn VARCHAR(50),
    name VARCHAR(100),
    description VARCHAR,
    language VARCHAR(50),
    released VARCHAR(11),
    author VARCHAR(100),
    publisher VARCHAR(100),
    category VARCHAR(50),
    PRIMARY KEY (isbn)
)